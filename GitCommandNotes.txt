Something cool here
and other stuff.

git clone [link]
git add [file.fileextension]
git commit -m "Your message."

git config user.name "Dancinpeter"
git config user.email thompsonpe@mnstate.edu

git status

git commit -a -m "Your message"
git push origin master
get pull origin master

add --> commit --> pull --> push

git branch
	Checks what branches exist
git branch [branch-name]
	Creates branch
git checkout [branch-name]
	Goes to most current commit of [branch-name]
	
git merge [branch-name]
